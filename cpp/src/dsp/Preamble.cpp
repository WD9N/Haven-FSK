#include "Preamble.h"

namespace HavenFSK {

std::vector<float> Preamble::generate() { return {}; }
double Preamble::correlate(const std::vector<float>&, int) { return 0.0; }

} // namespace HavenFSK
